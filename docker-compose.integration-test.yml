version: '3.8'

services:
  # PostgreSQL (for Java Ledger)
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: gtelpay_prod
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # RabbitMQ (message queue)
  rabbitmq:
    image: rabbitmq:3.12-management-alpine
    environment:
      RABBITMQ_DEFAULT_USER: gtel-c-server
      RABBITMQ_DEFAULT_PASS: password
      RABBITMQ_DEFAULT_VHOST: /gtel-prod
    ports:
      - "5672:5672"      # AMQP
      - "15672:15672"    # Management UI
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq

  # Redis (cache + dedup store)
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - redis_data:/data

  # C-Server (Wallet + Blockchain Listener) — stub mode
  c-server:
    build:
      context: ./c-server
      dockerfile: Dockerfile
    environment:
      C_SERVER_PORT: 8080
      BLOCKCHAIN_RPC_URL: https://eth-mainnet.g.alchemy.com/v2/demo
      RABBITMQ_URL: amqp://gtel-c-server:password@rabbitmq:5672/gtel-prod
      CUSTODY_WALLET: "0x1234567890123456789012345678901234567890"
    ports:
      - "8080:8080"
    depends_on:
      rabbitmq:
        condition: service_healthy

  # Java Ledger (COA accounting)
  java-ledger:
    build:
      context: ./java
      dockerfile: Dockerfile.ledger
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/gtelpay_prod
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_PORT: 5672
      SPRING_RABBITMQ_USERNAME: gtel-c-server
      SPRING_RABBITMQ_PASSWORD: password
      SPRING_RABBITMQ_VIRTUAL_HOST: /gtel-prod
      SPRING_REDIS_HOST: redis
      SPRING_REDIS_PORT: 6379
      SPRING_PROFILES_ACTIVE: ledger
    ports:
      - "8090:8090"
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

volumes:
  postgres_data:
  rabbitmq_data:
  redis_data:

networks:
  default:
    name: gtel-integration-test
