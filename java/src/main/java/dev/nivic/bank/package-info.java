/**
 * <strong>Bank</strong> and external rails integration: ISO 8583 (or host-specific) message handling,
 * acquirer/issuer adapters, HSM/crypto hooks, and mapping into Core — kept separate from internal
 * persistence and WAL orchestration in {@link dev.nivic.payment}.
 */
package dev.nivic.bank;
