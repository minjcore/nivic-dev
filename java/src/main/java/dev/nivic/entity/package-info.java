/**
 * Domain and persistence <strong>entities</strong>: types that mirror tables or stable aggregate roots
 * (e.g. wallet ledger rows, payment intent rows, merchant config). Prefer immutable records where
 * possible; keep transport-specific shapes in {@code dev.nivic.sevlet}.
 */
package dev.nivic.entity;
