/**
 * Domain <strong>models</strong>: value objects, invariants, and non-table types (amounts, policies,
 * command views) that are not servlet wire shapes ({@code sevlet}) or row-mapped entities
 * ({@code entity}). Use {@code dev.nivic.api} for outward JSON/DTO contracts.
 */
package dev.nivic.model;
