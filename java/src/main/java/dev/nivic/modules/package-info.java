/**
 * Optional <strong>feature modules</strong> (bounded slices of the app): add subpackages such as
 * {@code dev.nivic.modules.xxx} when a feature is large enough to isolate from {@code payment},
 * {@code sevlet}, or {@code ledger}. This root package holds no concrete types by default.
 */
package dev.nivic.modules;
