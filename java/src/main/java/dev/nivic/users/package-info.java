/**
 * <strong>User</strong> identity and profiles (people/operators/end-customers): accounts, roles,
 * sessions — separate from merchant {@code mid} configuration in {@code dev.nivic.sevlet.secret}
 * and {@code dev.nivic.merchant}. Add concrete types here when introducing user-bound auth beyond
 * HMAC-per-mid on the wallet servlet.
 */
package dev.nivic.users;
