# Classpath defaults. Precedence: OS env > Java -DsystemProperty > this file.
# Do not commit real secrets; use .env or env vars in production.

# --- PostgreSQL (required for webapp startup) ---
#JDBC_URL=jdbc:postgresql://127.0.0.1:5432/nivic
#JDBC_USER=nivic
#JDBC_PASSWORD=
#JDBC_POOL_SIZE=10

# --- HTTP proxy (Merchants UI under /httpproxy) — after servlet init-param ---
#HTTP_PROXY_TARGET_BASE=http://127.0.0.1:8080

# --- WAL signing (optional) ---
#NIVIC_WAL_SIGNING_KEY_PATH=
#NIVIC_WAL_SIGNING_KEY_B64=
